import {pizzaBases} from '../pizzaStore/pizzaBases'
import {pizzaSauces} from '../pizzaStore/pizzaSauces'
import {pizzaToppings} from '../pizzaStore/pizzaToppings'

export const pizzaBasess =  (state = pizzaBases, action) => {
  return state
}

export const pizzaSaucess = (state = pizzaSauces, action) => {
  return state
}

export const pizzaToppingss = (state = pizzaToppings, action) => {
  return state
}
